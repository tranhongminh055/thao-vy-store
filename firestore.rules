rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporarily permissive rules for testing
    // TODO: Tighten security after debugging
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

